<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.patsc.fallstudie">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <!--
<uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    -->

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.IntroductionActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.ForschungActivity" />
        <activity android:name=".Activities.ArmbandActivity" />
        <activity android:name=".Activities.UhrwerkActivity" />
        <activity android:name=".Activities.GehaeuseActivity" />
        <activity android:name=".Activities.BezahlartActivity" />
        <activity android:name=".Activities.WasserdichtheitActivity" />
        <activity android:name=".Activities.ZeitarbeiterActivity" />
        <activity android:name=".Activities.MarketingActivity" />
        <activity android:name=".Activities.ProduktionsvolumenActivity" />
        <activity android:name=".Activities.VerkaufspreisActivity" />
        <activity android:name=".Activities.BestellzusammenfassungActivity" />
        <activity android:name=".Activities.Z1_ArmbandActivity" />
        <activity android:name=".Activities.Z2_GehaeuseActivity" />
        <activity android:name=".Activities.Z3_ZusammenbauActivity" />
        <activity android:name=".Activities.BerechnungActivity" />
        <activity android:name=".Activities.RundenergebnisActivity" />
        <activity android:name=".Activities.BestenlisteActivity" />
        <activity android:name=".Activities.PersonalwesenActivity"></activity>
    </application>

</manifest>